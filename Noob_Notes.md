# Commands
## Clone Repo
`git clone {repo name}`

## Run Applications
`cd {dir name}`
`npm install`
`npm run start`

## Pull Changes
`git pull`

## Push Changes
`git add -A && git commit -m "{enter message here}"`
`git push`

